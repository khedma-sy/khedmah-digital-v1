'use client';
import Link from 'next/link';
import { useEffect,useState } from 'react';
import { useRouter } from 'next/navigation';
import { api,type AdminOrderMonitor } from '../../../lib/api-client';
const statusLabel=(value:string)=>({placed:'جديد',quoted:'تم التسعير',merchant_confirmed:'أكده المطعم',courier_assigned:'أُسند لمندوب',courier_accepted:'قبله المندوب',ready_for_pickup:'جاهز للاستلام',picked_up:'قيد التوصيل',delivered:'مُسلّم',rejected:'مرفوض',cancelled:'ملغى'}[value]??value);
const verticalLabel=(value:string)=>({food:'طعام',grocery:'بقالة',pharmacy:'صيدلية'}[value]??value);
export default function AdminOrdersPage(){const router=useRouter();const[data,setData]=useState<AdminOrderMonitor|null>(null);const[error,setError]=useState('');const[loading,setLoading]=useState(true);const load=()=>{setLoading(true);void api.operationsProduct.orderMonitor().then(result=>{setData(result.orderMonitor);setError('')}).catch(cause=>{const status=cause instanceof Error?(cause as Error&{statusCode?:number}).statusCode:undefined;if(status===401){router.replace('/auth/login?next=%2Fadmin%2Forders');return;}setError(status===403?'هذا الحساب لا يملك صلاحية مراقبة الطلبات.':cause instanceof Error?cause.message:'تعذر تحميل الطلبات.');}).finally(()=>setLoading(false));};useEffect(load,[]);return <main id="foundation-content" className="operations-shell" dir="rtl"><header className="operations-header"><div><p className="eyebrow">خدمة · التشغيل اللوجستي</p><h1>مراقبة الطلبات والتوصيل</h1><p>عرض تشغيلي محدود البيانات لا يكشف هاتف العميل أو عنوانه أو إحداثياته.</p></div><button onClick={load} disabled={loading}>تحديث</button></header><nav className="admin-navigation"><Link href="/admin">لوحة الإدارة</Link><Link href="/admin/operations-product">مركز التشغيل</Link></nav>{loading?<section className="operations-panel" aria-busy="true">جارٍ تحميل الطلبات…</section>:error?<section className="operations-panel"><p className="form-error" role="alert">{error}</p></section>:data?<><section className="operations-summary"><article><strong>{data.summary.active}</strong><span>طلبات نشطة</span></article><article><strong>{data.summary.stale}</strong><span>متأخرة أكثر من 30 دقيقة</span></article><article><strong>{data.summary.unassigned}</strong><span>دون مندوب</span></article><article><strong>{data.summary.total}</strong><span>ضمن آخر 200 طلب</span></article></section><section className="operations-panel"><div className="panel-heading"><h2>طابور المتابعة</h2><span>آخر تحديث {new Date(data.generatedAt).toLocaleTimeString('ar-SY')}</span></div>{data.orders.length===0?<p>لا توجد طلبات مسجلة بعد.</p>:<div className="moderation-list">{data.orders.map(order=><article className="moderation-card" key={order.id}><div><h3>{order.merchantName} · {verticalLabel(order.vertical)}</h3><p>{statusLabel(order.status)}{order.stale?' · متأخر ويحتاج متابعة':''}</p><small><bdi>{order.id}</bdi> · {order.courierName?`المندوب: ${order.courierName}`:'لم يُسند لمندوب'} · {order.eventCount} تحديثات</small></div><span className="status-badge">{order.total?`${order.total.toLocaleString('ar-SY')} ${order.currency}`:'السعر غير مكتمل'}</span></article>)}</div>}</section></>:null}</main>}
