'use client';
import { useEffect,useState } from 'react';
import { api,type FulfillmentOrderStatus } from '../../lib/api-client';
export function OrderTracking({orderId,status}:{orderId:string;status:FulfillmentOrderStatus}){const[location,setLocation]=useState<{latitude:number;longitude:number;recordedAt:string}|undefined>();useEffect(()=>{if(!['courier_accepted','ready_for_pickup','picked_up'].includes(status))return;let active=true;const load=()=>void api.orders.tracking(orderId).then(r=>{if(active)setLocation(r.location)}).catch(()=>undefined);load();const timer=window.setInterval(load,15000);return()=>{active=false;window.clearInterval(timer)}},[orderId,status]);if(!location)return <p>بانتظار مشاركة موقع المندوب.</p>;return <p><a href={`https://www.google.com/maps?q=${location.latitude},${location.longitude}`} target="_blank" rel="noopener noreferrer">متابعة موقع المندوب على الخريطة</a> · آخر تحديث {new Date(location.recordedAt).toLocaleTimeString('ar-SY')}</p>}
