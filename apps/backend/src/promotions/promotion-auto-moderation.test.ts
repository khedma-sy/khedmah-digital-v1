import assert from 'node:assert/strict';import test from 'node:test';
import type { BusinessProfile } from '../business-profiles/business-profile.types';import type { Promotion } from './promotion.types';import { evaluatePromotionAutoModeration } from './promotion-auto-moderation';
const now=new Date(),business:BusinessProfile={id:'b',name:'نشاط',ownerUserId:'o',visibility:'public',moderationStatus:'approved',trustStatus:'approved',status:'active',categoryCode:'restaurants',cityCode:'damascus',countryCode:'SY',isFeatured:false,createdAt:now.toISOString(),updatedAt:now.toISOString()};
const promotion:Promotion={id:'p',businessProfileId:'b',ownerUserId:'o',titleAr:'خصم وجبة العائلة',descriptionAr:'خصم واضح على وجبة العائلة داخل المطعم.',discountType:'percentage',originalPrice:100,discountValue:20,finalPrice:80,currency:'SYP',startsAt:now.toISOString(),endsAt:new Date(now.getTime()+7*86400000).toISOString(),totalLimit:100,perUserLimit:1,redeemedCount:0,status:'active',moderationStatus:'pending',createdAt:now.toISOString(),updatedAt:now.toISOString()};
test('eligible bounded promotion is automatically approved',()=>assert.equal(evaluatePromotionAutoModeration(promotion,business).approved,true));
test('high discount, long campaign, sensitive content and untrusted business require review',()=>{const d=evaluatePromotionAutoModeration({...promotion,titleAr:'دواء بوصفة',discountValue:70,endsAt:new Date(now.getTime()+40*86400000).toISOString()},{...business,trustStatus:'pending'});for(const reason of ['business_not_eligible','high_discount','long_campaign','high_risk_content'])assert.ok(d.reasons.includes(reason));});
