export type DiscountType = 'percentage'|'fixed_amount';
export interface Promotion { id:string; businessProfileId:string; businessName?:string; ownerUserId:string; titleAr:string; descriptionAr:string; discountType:DiscountType; originalPrice:number; discountValue:number; finalPrice:number; currency:'SYP'|'USD'; startsAt:string; endsAt:string; totalLimit:number; perUserLimit:number; redeemedCount:number; status:'active'|'inactive'; moderationStatus:'pending'|'approved'|'rejected'; moderationPolicyVersion?:string; rejectionReason?:string; createdAt:string; updatedAt:string; }
export interface PromotionClaim { id:string; promotionId:string; titleAr?:string; businessName?:string; userId:string; redemptionCode:string; status:'issued'|'redeemed'|'expired'|'cancelled'; expiresAt:string; redeemedAt?:string; createdAt:string; }
