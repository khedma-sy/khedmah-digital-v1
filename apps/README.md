# Apps

Mission 007 initializes the executable platform foundation applications:

- `backend/` — NestJS and TypeScript platform foundation with `GET /api/v1/health`.
- `frontend/` — Next.js, React, and TypeScript Arabic-first RTL foundation.

No product features, business logic, users, authentication, organizations, business profiles, categories, locations, search, marketplace, messaging, payments, analytics features, Khedmah Connect, or community features are implemented.
