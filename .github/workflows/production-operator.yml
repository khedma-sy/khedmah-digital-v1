name: Production Operator - RETIRED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse legacy production path
        shell: bash
        run: |
          echo 'This legacy Production workflow is retired and intentionally cannot authenticate or deploy.' >&2
          echo 'Use production-operator-new-account.yml for deployment and production-migrations-025-034.yml for migrations 025-034.' >&2
          exit 1
