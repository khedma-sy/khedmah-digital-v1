name: Production Operator - New Account

on:
  workflow_dispatch:
    inputs:
      mode:
        description: Operation to perform
        required: true
        default: VERIFY_ONLY
        type: choice
        options: [VERIFY_ONLY, DEPLOY_PRODUCTION]
      commit_sha:
        description: Exact latest main commit SHA for deployment
        required: false
        type: string

permissions:
  contents: read
  id-token: write

env:
  GOOGLE_CLOUD_PROJECT: ${{ vars.GOOGLE_CLOUD_PROJECT }}
  PRODUCTION_GOOGLE_CLOUD_PROJECT: ${{ vars.PRODUCTION_GOOGLE_CLOUD_PROJECT }}
  GOOGLE_CLOUD_REGION: ${{ vars.GOOGLE_CLOUD_REGION }}
  OPERATIONS_ARTIFACT_REPOSITORY: ${{ vars.OPERATIONS_ARTIFACT_REPOSITORY || 'khedmah-digital' }}
  OPERATIONS_BACKEND_SERVICE: ${{ vars.OPERATIONS_BACKEND_SERVICE || 'backend' }}
  OPERATIONS_FRONTEND_SERVICE: ${{ vars.OPERATIONS_FRONTEND_SERVICE || 'frontend' }}
  OPERATIONS_RUNTIME_SERVICE_ACCOUNT: ${{ vars.OPERATIONS_RUNTIME_SERVICE_ACCOUNT }}
  OPERATIONS_BUILD_SERVICE_ACCOUNT: ${{ vars.OPERATIONS_BUILD_SERVICE_ACCOUNT }}
  OPERATIONS_MIGRATION_SERVICE_ACCOUNT: ${{ vars.OPERATIONS_MIGRATION_SERVICE_ACCOUNT }}
  DATABASE_RUNTIME_USER: ${{ vars.DATABASE_RUNTIME_USER || 'khedmah_app' }}
  DATABASE_MIGRATION_USER: ${{ vars.DATABASE_MIGRATION_USER || 'khedmah_migrator' }}
  DATABASE_RUNTIME_ROLE: ${{ vars.DATABASE_RUNTIME_ROLE || 'khedmah_runtime_role' }}
  DATABASE_MIGRATION_ROLE: ${{ vars.DATABASE_MIGRATION_ROLE || 'khedmah_migration_role' }}
  DATABASE_NAME: ${{ vars.DATABASE_NAME || 'khedmah' }}
  CLOUD_SQL_INSTANCE_CONNECTION_NAME: ${{ vars.CLOUD_SQL_INSTANCE_CONNECTION_NAME }}
  GCS_MEDIA_BUCKET: ${{ vars.GCS_MEDIA_BUCKET }}
  CORS_ORIGIN: ${{ vars.CORS_ORIGIN }}
  NEXT_PUBLIC_SITE_URL: ${{ vars.NEXT_PUBLIC_SITE_URL }}
  EMAIL_FROM: ${{ vars.EMAIL_FROM }}
  GCS_MEDIA_LOCATION: ${{ vars.GCS_MEDIA_LOCATION }}
  GOOGLE_LOGGING_ENABLED: ${{ vars.GOOGLE_LOGGING_ENABLED }}
  GOOGLE_MONITORING_ENABLED: ${{ vars.GOOGLE_MONITORING_ENABLED }}
  GOOGLE_ERROR_REPORTING_ENABLED: ${{ vars.GOOGLE_ERROR_REPORTING_ENABLED }}
  FACEBOOK_AUTH_ENABLED: ${{ vars.FACEBOOK_AUTH_ENABLED || 'false' }}
  CLASSIFIEDS_ENABLED: ${{ vars.CLASSIFIEDS_ENABLED }}
  NEXT_PUBLIC_CLASSIFIEDS_ENABLED: ${{ vars.NEXT_PUBLIC_CLASSIFIEDS_ENABLED }}
  TAXI_ACCESS_ENABLED: ${{ vars.TAXI_ACCESS_ENABLED }}
  TAXI_TRIPS_ENABLED: ${{ vars.TAXI_TRIPS_ENABLED }}
  TAXI_OPERATING_ZONE: ${{ vars.TAXI_OPERATING_ZONE }}
  REQUESTED_SHA: ${{ inputs.commit_sha }}

jobs:
  verify:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Validate protected new-account variables
        shell: bash
        run: |
          set -euo pipefail
          for name in GOOGLE_CLOUD_PROJECT PRODUCTION_GOOGLE_CLOUD_PROJECT GOOGLE_CLOUD_REGION OPERATIONS_ARTIFACT_REPOSITORY OPERATIONS_BACKEND_SERVICE OPERATIONS_FRONTEND_SERVICE OPERATIONS_RUNTIME_SERVICE_ACCOUNT OPERATIONS_BUILD_SERVICE_ACCOUNT OPERATIONS_MIGRATION_SERVICE_ACCOUNT CLOUD_SQL_INSTANCE_CONNECTION_NAME GCS_MEDIA_BUCKET CORS_ORIGIN NEXT_PUBLIC_SITE_URL EMAIL_FROM GCS_MEDIA_LOCATION GOOGLE_LOGGING_ENABLED GOOGLE_MONITORING_ENABLED GOOGLE_ERROR_REPORTING_ENABLED CLASSIFIEDS_ENABLED NEXT_PUBLIC_CLASSIFIEDS_ENABLED TAXI_ACCESS_ENABLED TAXI_TRIPS_ENABLED TAXI_OPERATING_ZONE; do
            test -n "${!name:-}" || { echo "Missing production variable: $name" >&2; exit 1; }
          done
          test "$GOOGLE_CLOUD_PROJECT" = "$PRODUCTION_GOOGLE_CLOUD_PROJECT"
          legacy_project="project-""94512a0e-1a5e-4bdb-87f"
          legacy_number="774201""339973"
          for value in "$GOOGLE_CLOUD_PROJECT" "$OPERATIONS_RUNTIME_SERVICE_ACCOUNT" "$OPERATIONS_BUILD_SERVICE_ACCOUNT" "$CLOUD_SQL_INSTANCE_CONNECTION_NAME" "$GCS_MEDIA_BUCKET" "$CORS_ORIGIN" "$NEXT_PUBLIC_SITE_URL"; do
            [[ "$value" != *"$legacy_project"* && "$value" != *"$legacy_number"* ]]
          done
          [[ "$OPERATIONS_RUNTIME_SERVICE_ACCOUNT" == *"@${GOOGLE_CLOUD_PROJECT}.iam.gserviceaccount.com" ]]
          [[ "$OPERATIONS_BUILD_SERVICE_ACCOUNT" == *"@${GOOGLE_CLOUD_PROJECT}.iam.gserviceaccount.com" ]]
          [[ "$OPERATIONS_MIGRATION_SERVICE_ACCOUNT" == *"@${GOOGLE_CLOUD_PROJECT}.iam.gserviceaccount.com" ]]
          [[ "$CLOUD_SQL_INSTANCE_CONNECTION_NAME" == "${GOOGLE_CLOUD_PROJECT}:${GOOGLE_CLOUD_REGION}:"* ]]
          [[ "$NEXT_PUBLIC_SITE_URL" == https://* ]]
          [[ "$NEXT_PUBLIC_SITE_URL" != *","* ]]
          [[ "$NEXT_PUBLIC_SITE_URL" != *"localhost"* && "$NEXT_PUBLIC_SITE_URL" != *"127.0.0.1"* ]]
          test "$GOOGLE_LOGGING_ENABLED" = true
          test "$GOOGLE_MONITORING_ENABLED" = true
          test "$GOOGLE_ERROR_REPORTING_ENABLED" = true
          [[ "$CLASSIFIEDS_ENABLED" == "true" || "$CLASSIFIEDS_ENABLED" == "false" ]]
          [[ "$NEXT_PUBLIC_CLASSIFIEDS_ENABLED" == "true" || "$NEXT_PUBLIC_CLASSIFIEDS_ENABLED" == "false" ]]
          [[ "$NEXT_PUBLIC_CLASSIFIEDS_ENABLED" != "true" || "$CLASSIFIEDS_ENABLED" == "true" ]] || {
            echo 'Frontend Classifieds cannot be enabled while Backend Classifieds is disabled.' >&2
            exit 1
          }
          [[ "$TAXI_ACCESS_ENABLED" == "true" || "$TAXI_ACCESS_ENABLED" == "false" ]]
          [[ "$TAXI_TRIPS_ENABLED" == "true" || "$TAXI_TRIPS_ENABLED" == "false" ]]
          [[ "$TAXI_OPERATING_ZONE" =~ ^[A-Za-z0-9_-]{1,50}$ ]] || {
            echo 'TAXI_OPERATING_ZONE is invalid.' >&2
            exit 1
          }
          [[ "$TAXI_TRIPS_ENABLED" != "true" || "$TAXI_ACCESS_ENABLED" == "true" ]] || {
            echo 'Taxi trips cannot be enabled while Taxi access is disabled.' >&2
            exit 1
          }

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.GCP_PRODUCTION_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.OPERATIONS_DEPLOYER_SERVICE_ACCOUNT }}

      - uses: google-github-actions/setup-gcloud@v3

      - name: Verify Google/Firebase/operations readiness
        shell: bash
        env:
          OPERATIONS_DEPLOYER_SERVICE_ACCOUNT: ${{ secrets.OPERATIONS_DEPLOYER_SERVICE_ACCOUNT }}
          OPERATIONS_APPROVED_PRODUCTION: 'true'
          ALLOW_FIRST_PRODUCTION_DEPLOY: ${{ inputs.mode == 'DEPLOY_PRODUCTION' && 'true' || 'false' }}
          OPERATIONS_PRODUCT_ROLE_BINDINGS: ${{ secrets.OPERATIONS_PRODUCT_ROLE_BINDINGS }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          FIREBASE_PROJECT_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}
          FIREBASE_API_KEY: ${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}
          FIREBASE_APP_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}
          FIREBASE_STORAGE_BUCKET: ${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_MEASUREMENT_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID }}
          NEXT_PUBLIC_FIREBASE_API_KEY: ${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
          NEXT_PUBLIC_FIREBASE_APP_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID }}
          NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID }}
          NEXT_PUBLIC_GOOGLE_MAPS_API_KEY: ${{ secrets.GOOGLE_MAPS_BROWSER_API_KEY }}
        run: |
          set -euo pipefail
          gcloud config set project "$GOOGLE_CLOUD_PROJECT"
          gcloud projects describe "$GOOGLE_CLOUD_PROJECT" --format='value(projectId)'
          npm ci
          npm run validate:google
          npm run validate:firebase
          npm run validate:identity:production
          npm run validate:operations:production
          bash scripts/validate-production-deployment-readiness.sh

      - name: Verify production database role isolation before deployment
        if: inputs.mode == 'DEPLOY_PRODUCTION'
        shell: bash
        run: |
          set -euo pipefail
          [[ "$REQUESTED_SHA" =~ ^[0-9a-f]{40}$ ]]
          git fetch origin main
          test "$(git rev-parse origin/main)" = "$REQUESTED_SHA"
          test "$GITHUB_SHA" = "$REQUESTED_SHA"
          test "$(gcloud secrets versions describe latest --secret DATABASE_MIGRATION_URL --project "$GOOGLE_CLOUD_PROJECT" --format='value(state)')" = ENABLED
          BUILD_SERVICE_ACCOUNT="projects/$GOOGLE_CLOUD_PROJECT/serviceAccounts/$OPERATIONS_BUILD_SERVICE_ACCOUNT"
          gcloud builds submit . \
            --project "$GOOGLE_CLOUD_PROJECT" \
            --region "$GOOGLE_CLOUD_REGION" \
            --service-account "$BUILD_SERVICE_ACCOUNT" \
            --gcs-source-staging-dir "gs://$GOOGLE_CLOUD_PROJECT-cloudbuild-source/source" \
            --config cloudbuild.database-role-bootstrap.yaml \
            --substitutions "COMMIT_SHA=$REQUESTED_SHA,_REGION=$GOOGLE_CLOUD_REGION,_AR_REPOSITORY=$OPERATIONS_ARTIFACT_REPOSITORY" \
            --quiet
          IMAGE="$GOOGLE_CLOUD_REGION-docker.pkg.dev/$GOOGLE_CLOUD_PROJECT/$OPERATIONS_ARTIFACT_REPOSITORY/database-role-bootstrap:$REQUESTED_SHA"
          JOB="khedmah-database-role-predeploy-verify"
          gcloud run jobs deploy "$JOB" \
            --project "$GOOGLE_CLOUD_PROJECT" \
            --region "$GOOGLE_CLOUD_REGION" \
            --image "$IMAGE" \
            --service-account "$OPERATIONS_MIGRATION_SERVICE_ACCOUNT" \
            --set-cloudsql-instances "$CLOUD_SQL_INSTANCE_CONNECTION_NAME" \
            --set-secrets 'DATABASE_URL=DATABASE_MIGRATION_URL:latest' \
            --set-env-vars "DATABASE_ROLE_PHASE=verify,DATABASE_RUNTIME_USER=$DATABASE_RUNTIME_USER,DATABASE_MIGRATION_USER=$DATABASE_MIGRATION_USER,DATABASE_RUNTIME_ROLE=$DATABASE_RUNTIME_ROLE,DATABASE_MIGRATION_ROLE=$DATABASE_MIGRATION_ROLE,DATABASE_NAME=$DATABASE_NAME" \
            --tasks 1 --parallelism 1 --max-retries 0 --task-timeout 10m --quiet
          gcloud run jobs execute "$JOB" \
            --project "$GOOGLE_CLOUD_PROJECT" \
            --region "$GOOGLE_CLOUD_REGION" \
            --wait

  deploy:
    if: ${{ inputs.mode == 'DEPLOY_PRODUCTION' }}
    needs: verify
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.commit_sha }}
          fetch-depth: 0

      - name: Lock deployment to exact latest main
        shell: bash
        run: |
          set -euo pipefail
          [[ "$REQUESTED_SHA" =~ ^[0-9a-f]{40}$ ]]
          git fetch origin main
          test "$(git rev-parse HEAD)" = "$REQUESTED_SHA"
          test "$(git rev-parse origin/main)" = "$REQUESTED_SHA"

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.GCP_PRODUCTION_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.OPERATIONS_DEPLOYER_SERVICE_ACCOUNT }}

      - uses: google-github-actions/setup-gcloud@v3

      - uses: actions/setup-node@v7
        with:
          node-version: 24

      - id: before
        name: Capture currently serving Production revision pair
        shell: bash
        run: |
          set -euo pipefail
          backend_revision="$(gcloud run services describe "$OPERATIONS_BACKEND_SERVICE"             --project "$GOOGLE_CLOUD_PROJECT" --region "$GOOGLE_CLOUD_REGION"             --format='value(status.traffic[0].revisionName)' 2>/dev/null || true)"
          frontend_revision="$(gcloud run services describe "$OPERATIONS_FRONTEND_SERVICE"             --project "$GOOGLE_CLOUD_PROJECT" --region "$GOOGLE_CLOUD_REGION"             --format='value(status.traffic[0].revisionName)' 2>/dev/null || true)"
          if [[ -n "$backend_revision" && -n "$frontend_revision" ]]; then
            echo "rollback_available=true" >> "$GITHUB_OUTPUT"
            echo "backend_revision=$backend_revision" >> "$GITHUB_OUTPUT"
            echo "frontend_revision=$frontend_revision" >> "$GITHUB_OUTPUT"
          else
            echo "rollback_available=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Deploy exact main commit through account-neutral Cloud Build
        shell: bash
        run: |
          set -euo pipefail
          gcloud config set project "$GOOGLE_CLOUD_PROJECT"
          BUILD_SERVICE_ACCOUNT="projects/${GOOGLE_CLOUD_PROJECT}/serviceAccounts/${OPERATIONS_BUILD_SERVICE_ACCOUNT}"
          gcloud builds submit . \
            --project "$GOOGLE_CLOUD_PROJECT" \
            --region "$GOOGLE_CLOUD_REGION" \
            --service-account "$BUILD_SERVICE_ACCOUNT" \
            --gcs-source-staging-dir "gs://${GOOGLE_CLOUD_PROJECT}-cloudbuild-source/source" \
            --config cloudbuild.production-new-account.yaml \
            --substitutions "COMMIT_SHA=$REQUESTED_SHA,_REGION=$GOOGLE_CLOUD_REGION,_AR_REPOSITORY=$OPERATIONS_ARTIFACT_REPOSITORY,_BACKEND_SERVICE=$OPERATIONS_BACKEND_SERVICE,_FRONTEND_SERVICE=$OPERATIONS_FRONTEND_SERVICE,_RUNTIME_SERVICE_ACCOUNT=$OPERATIONS_RUNTIME_SERVICE_ACCOUNT,_CLOUD_SQL_INSTANCE=$CLOUD_SQL_INSTANCE_CONNECTION_NAME,_GCS_MEDIA_BUCKET=$GCS_MEDIA_BUCKET,_CORS_ORIGIN=$CORS_ORIGIN,_SITE_URL=$NEXT_PUBLIC_SITE_URL,_EMAIL_FROM=$EMAIL_FROM,_GOOGLE_LOGGING_ENABLED=$GOOGLE_LOGGING_ENABLED,_GOOGLE_MONITORING_ENABLED=$GOOGLE_MONITORING_ENABLED,_GOOGLE_ERROR_REPORTING_ENABLED=$GOOGLE_ERROR_REPORTING_ENABLED,_FACEBOOK_AUTH_ENABLED=$FACEBOOK_AUTH_ENABLED,_CLASSIFIEDS_ENABLED=$CLASSIFIEDS_ENABLED,_NEXT_PUBLIC_CLASSIFIEDS_ENABLED=$NEXT_PUBLIC_CLASSIFIEDS_ENABLED,_TAXI_ACCESS_ENABLED=$TAXI_ACCESS_ENABLED,_TAXI_TRIPS_ENABLED=$TAXI_TRIPS_ENABLED,_TAXI_OPERATING_ZONE=$TAXI_OPERATING_ZONE" \
            --quiet

      - name: Verify live revisions and Maps readiness
        shell: bash
        run: |
          set -euo pipefail
          BACKEND_URL="$(gcloud run services describe "$OPERATIONS_BACKEND_SERVICE" --project "$GOOGLE_CLOUD_PROJECT" --region "$GOOGLE_CLOUD_REGION" --format='value(status.url)')"
          FRONTEND_URL="$(gcloud run services describe "$OPERATIONS_FRONTEND_SERVICE" --project "$GOOGLE_CLOUD_PROJECT" --region "$GOOGLE_CLOUD_REGION" --format='value(status.url)')"
          curl --fail --silent --show-error --retry 6 --retry-all-errors "$BACKEND_URL/api/v1/health" >/dev/null
          curl --fail --silent --show-error --retry 6 --retry-all-errors "$BACKEND_URL/api/v1/health/ready" >/dev/null
          curl --fail --silent --show-error --retry 6 --retry-all-errors "$FRONTEND_URL/" >/dev/null
          bash scripts/validate-firebase-social-auth-readiness.sh
          BROWSER="$(command -v google-chrome || command -v google-chrome-stable || true)"
          test -n "$BROWSER" || { echo 'Chrome is required for live Maps certification.' >&2; exit 1; }
          MAP_DOM="$($BROWSER --headless --no-sandbox --disable-gpu --virtual-time-budget=25000 --dump-dom "$FRONTEND_URL/map")"
          printf '%s' "$MAP_DOM" | grep -F 'data-map-status="ready"' >/dev/null
          echo "BACKEND_URL=$BACKEND_URL" >> "$GITHUB_STEP_SUMMARY"
          echo "FRONTEND_URL=$FRONTEND_URL" >> "$GITHUB_STEP_SUMMARY"
          echo "Android/clients API variable after approval: NEXT_PUBLIC_API_URL=$BACKEND_URL" >> "$GITHUB_STEP_SUMMARY"
          echo 'MAP_STATUS=ready' >> "$GITHUB_STEP_SUMMARY"

      - name: Emergency rollback after failed Production deployment or certification
        if: failure() && steps.before.outputs.rollback_available == 'true'
        env:
          OPERATIONS_APPROVED_PRODUCTION: 'true'
          PREVIOUS_BACKEND_REVISION: ${{ steps.before.outputs.backend_revision }}
          PREVIOUS_FRONTEND_REVISION: ${{ steps.before.outputs.frontend_revision }}
        shell: bash
        run: |
          set -euo pipefail
          bash scripts/google-production-rollback.sh "$PREVIOUS_BACKEND_REVISION" "$PREVIOUS_FRONTEND_REVISION"
          echo 'EMERGENCY_ROLLBACK=SUCCESS' >> "$GITHUB_STEP_SUMMARY"
