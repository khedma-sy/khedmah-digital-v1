# Firebase production foundation

Authentication, FCM, Analytics, Crashlytics, and Cloud Storage are reserved adapters. They remain disabled until production identifiers, consent/retention rules, and launch approval are complete. Configuration is environment-only. Native configuration files are ignored.
