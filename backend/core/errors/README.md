# Backend Core Errors Foundation

This folder contains the Mission 052 base error foundation.

Implemented foundation files:

- `base-error.mjs` provides `KhedmahCoreError`, common error categories, metadata support, and a validation-error helper.

This folder does not implement API responses, controllers, business errors for product modules, authentication, authorization, database access, or production infrastructure.
