# Locations Tests

Mission 060 location foundation tests live in the repository-level `tests/` folder and cover module structure, location concepts, hierarchy, coverage references, ownership, visibility, lifecycle, validation, dependency, security, audit, and KILL CRITICAL boundaries.
