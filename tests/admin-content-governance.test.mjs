import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import test from'node:test';const read=path=>readFile(new URL(`../${path}`,import.meta.url),'utf8');
test('admin exposes governed ad limits and auto moderation outcomes',async()=>{const[policy,productService,repository,service,controller,page]=await Promise.all([read('apps/backend/src/products/product-policy.ts'),read('apps/backend/src/products/product.service.ts'),read('apps/backend/src/operations-product/operations-product.repository.ts'),read('apps/backend/src/operations-product/operations-product.service.ts'),read('apps/backend/src/operations-product/operations-product.controller.ts'),read('apps/frontend/app/admin/moderation/page.tsx')]);assert.match(policy,/FREE_PRODUCT_LIMIT_PER_USER=3/);assert.match(policy,/PAID_ADVERTISING_ENABLED/);assert.match(policy,/PRODUCT_LISTING_LIMIT_PER_USER/);assert.match(productService,/productLimitPerUser/);assert.match(repository,/owners_at_limit/);assert.match(repository,/promotions_auto_approved_30d/);assert.match(service,/actor\(cookie,'security.manage'\)/);assert.match(controller,/@Get\('content-governance'\)/);assert.match(page,/حد الإعلانات لكل مستخدم/);assert.match(page,/سياسة القبول الآلي/);assert.match(page,/لا ينفذ الأدمن الذكي رفضًا استثنائيًا بمفرده/);});
