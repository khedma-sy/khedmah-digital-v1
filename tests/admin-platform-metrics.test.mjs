import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import test from'node:test';const read=path=>readFile(new URL(`../${path}`,import.meta.url),'utf8');
test('owner dashboard aggregates every live platform domain without personal data',async()=>{const[repository,service,controller,page,client]=await Promise.all([read('apps/backend/src/operations-product/operations-product.repository.ts'),read('apps/backend/src/operations-product/operations-product.service.ts'),read('apps/backend/src/operations-product/operations-product.controller.ts'),read('apps/frontend/app/admin/page.tsx'),read('apps/frontend/lib/api-client.ts')]);for(const table of['core_user_accounts','business_profiles','professional_profiles','product_listings','fulfillment_orders','mobility_requests','professional_service_requests','operations_incidents'])assert.match(repository,new RegExp(table));assert.match(repository,/aggregatedOnly:true/);assert.match(repository,/personalDataExposed:false/);assert.match(service,/platformMetrics/);assert.match(controller,/@Get\('platform-metrics'\)/);for(const label of['المستخدمون','الأنشطة والمهنيون','متجر خدمة','الطعام والتوصيل','التكسي والتنقل','الخدمات المهنية'])assert.match(page,new RegExp(label));assert.match(client,/AdminPlatformMetrics/);});
